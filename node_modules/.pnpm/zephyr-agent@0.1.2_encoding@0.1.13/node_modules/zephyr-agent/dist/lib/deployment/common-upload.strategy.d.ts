import type { UploadOptions, ZephyrEngine } from '../../zephyr-engine';
export declare function commonUploadStrategy(zephyr_engine: ZephyrEngine, { snapshot, getDashData, assets: { assetsMap, missingAssets } }: UploadOptions): Promise<string>;
