export declare class AuthListener {
    private workerUrl;
    constructor(workerUrl: URL);
    waitForToken(timeoutMs?: number): Promise<{
        sessionId: string;
        token: string;
    }>;
}
